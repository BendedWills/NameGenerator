# What is this?
A simple node.js app that takes a list of words and randomly combines them to create band names.
There is also a chance that the names will be combined with a prefix or a suffix to generate words that don't exist.
